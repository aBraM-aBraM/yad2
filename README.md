# Yad2 Telegram Bot

# Setup
```
git clone git@github.com:aBraM-aBraM/yad2.git
cd yad2

mkdir secrets
echo <your_telegram_bot_token> > secrets/bot_token.secret

pip install -r requirements.txt
python -m src.main --chrome-driver <chromedriver-path>
```
